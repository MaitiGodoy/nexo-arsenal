{
  "mcpServers": {
    "n8n": {
      "command": "npx",
      "args": ["-y", "n8n-mcp-server"],
      "env": {
        "N8N_API_URL": "${NEXO_N8N_URL}",
        "N8N_API_KEY": "${NEXO_N8N_API_KEY}"
      },
      "_instrucao": "Defina NEXO_N8N_URL e NEXO_N8N_API_KEY no .env do projeto. Usado para automações NEXO (cron, webhook, notificação)."
    }
  }
}
